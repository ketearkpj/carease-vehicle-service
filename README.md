# carease-vehicle-service
A service platform that incorporates car wash, car sale, car rental and car repair

## Run locally (one command)

```bash
./run-local.sh
```

This starts both:
- Backend: `http://localhost:5000`
- Frontend: `http://localhost:5173`
